import styled from 'styled-components'

export const DropdownWrapper = styled.div`
  position: relative;
`

export const DropdownPopover = styled.ul`
  position: absolute;
  right: 0;
  background-color: #ffffff;
  box-shadow: 0 3px 6px rgba(0, 0, 0, 0.16), 0 3px 6px rgba(0, 0, 0, 0.23);
  border-radius: 4px;
  z-index: 10;
`
